import { styled } from "@mui/material"

export const AvatarHero = styled("div")(() => ({
  backgroundColor: "black",
  height: "100vh",
}))
export const AvatarImg = styled("img")(() => ({
  borderRadius: "50%",
  width: "100%",
}))
